# Код читалки правится и в WSL, и из Windows. Без этого правила git при
# работе со стороны Windows подменяет переводы строк на CRLF, и в diff
# попадает весь файл вместо изменённых строк.
* text=auto eol=lf
